const { json } = require("express");
const express = require("express");
const router = express.Router();

const {addNewGuildId} = require('../controllers/guild-controllers');


// router.get("/:guildId", getGuildByGuildId);

router.post("/new", addNewGuildId);

// router.get("/:guildId/points", getGuildPointsByGuildId);

// router.post("/:guildId/predictions", createNewPredictionInGuildId);

// router.post("/:guildId/:userId", addNewUserInGuildByGuildId);

// router.delete("/:guildId/:userId", removeUserFromGuildByGuildId);

module.exports = router;
